import customKey from '../AppFiles/Functions/customKey';

const errorMessagesGlobals = {
  networkErrors: {
    key: `${customKey()}:::::${customKey()}:::::${customKey()}`,
  },
  notLoggedInErrors: {
    key: `${customKey()}:::::${customKey()}:::::${customKey()}`,
  },
  successMessage: {
    key: `${customKey()}:::::${customKey()}:::::${customKey()}`,
  },
  errorMessagesApp: {
    key: `${customKey()}:::::${customKey()}:::::${customKey()}`,
  },
};

export default errorMessagesGlobals;
